package func main    //Declares the package the code belongs to

import (             //Makes the format (fmt) package available for use
    "fmt"
)

func main() {        //Declares function called main
    fmt.Println("Hello, World!")         //Prints to screen
}

//No semicolons? format of open bracket, code, close bracket
//on each line will insert the semicolons during compilation
